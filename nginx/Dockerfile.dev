FROM nginx:1.27-alpine

RUN rm /etc/nginx/conf.d/default.conf
COPY dev.conf /etc/nginx/conf.d/app.conf

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
  CMD wget -q -O - http://localhost/api/health >/dev/null || exit 1
