# Copy to .env and fill in real values. docker-compose loads .env from the
# project root automatically and substitutes ${FPC_ADMIN_TOKEN} etc.

# Shared secret for the /admin panel. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
FPC_ADMIN_TOKEN=replace-me-with-a-long-random-string
